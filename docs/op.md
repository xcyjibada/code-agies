你正在为一个通用的 Bug Bounty 代码审计工具 code-agies 进行开发和优化。
核心原则（必须严格遵守）：

- 必须保持**高度通用性**，能有效支持各种 Python/JS/TS 项目，而不仅仅是当前测试的 zipp、InvokeAI、LibreChat 等靶子。
- 所有修改都必须以「大多数真实开源项目和 Bounty 程序」的平均情况为准。
- 禁止针对单个靶子的特性做硬编码或特殊优化。
- 如果某个优化只对 zipp / 某个特定靶子特别有效，必须明确标记为「zipp-specific hack」或「target-specific」，并提供通用 fallback 方案。
- 优先考虑「最小必要改动」+「最大泛化能力」。
- 在提出任何修改方案前，先思考：「这个改动在 LangChain、FastAPI、Django、Next.js、Flask 大项目上是否仍然合理？」

每次回复修改建议时，请在最后额外输出一段：
【泛化评估】：这个改动对其他项目的潜在影响是？可能引入的过拟合风险等级（低/中/高）及理由。