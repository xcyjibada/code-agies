还需要确认：

是否已有 GitHub Issue 或 Pull Request 描述相同问题；
是否已有安全公告；
是否已有维护者在其他渠道确认并计划修复；
你提到的 CVE-2024-5569 是否确实与当前问题不同（而不是同一根因的另一种触发方式）。

如果这些都排除了，那么"此前未公开"这一点才更有把握。

另外还有一点：

第 4 条：4 个并发即可 DoS

这一条非常有价值，但最好提供可重复的数据，例如：

Gunicorn 配置（worker 数、worker 类型）。
并发请求数。
正常请求在攻击期间的响应时间（例如从几十毫秒上升到数十秒或超时）。

这样维护者更容易判断影响程度。